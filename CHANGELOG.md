# Changelog

سابقهٔ انتشارهای دَفتَرچه.

شمارهٔ نسخه در `js/version.js` نوشته می‌شود (`DAFTARCHE_RELEASE`) و همین شماره در برنامه نمایش داده می‌شود. هر انتشار، یک بخش جدید بالای این فایل می‌گیرد و همراهش ورودی کوتاه دیالوگ «چی تغییر کرد؟» در `js/changelog.js` به‌روزرسانی می‌شود.

تغییرهای بخش **Unreleased** روی نسخهٔ فعلی نوشته شده‌اند اما هنوز منتشر نشده‌اند؛ هنگام انتشار، `RELEASE` و در صورت تغییر فایل‌های پوسته `BUILD` جلو می‌رود و همین بخش زیر شمارهٔ نسخهٔ جدید منتقل می‌شود.

## 2.1.0

### Added

- **صفحهٔ آمار، یک صفحه برای همهٔ بازهها.** آمار از پروفایل به یک صفحهٔ کامل رسید: پنج بازهٔ چرخشی (۷، ۳۰، ۹۰، ۱۸۰ و ۳۶۵ روز گذشته) با یک نوار انتخاب، حلقهٔ نرخ تکمیل همان بازه، نمودار روز/هفته/ماه و بخش‌های تمرکز، دسته‌ها، مطالعه، حال، بهترین‌ها و سابقهٔ تمرکز. بازهٔ انتخابی خواننده در `daftarche-stats-range` یادش می‌ماند.
- **بینش‌ها بخشی از همان صفحه شدند.** «چیزهایی که دیدیم» با خلاصه، الگوها و پیشنهاد داخل صفحهٔ آمار نشست و صفحهٔ جدایش برداشته شد؛ چهار کارت آماریِ پروفایل هم جایش را به یک کارت «آمار من» داد، پس آمار فقط یک دَر دارد.
- **کارت آمار برای اشتراک‌گذاری.** از بالای صفحهٔ آمار یک کارت مربعی ۱۰۸۰×۱۰۸۰ ساخته می‌شود — کاغذ و چسب، همدمِ خواننده، عنوان بازه، سه عدد، نمودار میله‌ای، تاریخ شمسی و یک جملهٔ پایانی — که با اشتراک‌گذاری سیستم فرستاده می‌شود و اگر مرورگر پشتیبانی نکند همان PNG ذخیره می‌شود.
- **عکس پروفایل شخصی، جدا از کاراکتر.** حالا می‌شود هم عکس خودت رو گذاشت و هم یه کاراکتر دفترچه انتخاب کرد. عکس روی همان دایرهٔ صفحهٔ پروفایل می‌نشیند و کاراکتر جای خودش را دارد، در آیکون بالای صفحه و کارت «کاراکتر من»؛ انتخاب یکی روی دیگری دست نمی‌زند. عکس فقط روی همین دستگاه می‌ماند، هنگام افزودن به یک مربع ۲۵۶ پیکسلی کوچک و فشرده تبدیل می‌شود (وگرنه یک عکس گوشی هیچ‌وقت در حافظهٔ مرورگر جا نمی‌شد)، و هر وقت خواستی با یک تپ حذف می‌شود.
- **انتخاب کاراکتر حالا انتخاب مشخصی است.** با آوردن نشانگر روی هر کاراکتر هاله‌ای دورش تشکیل می‌شود و بقیه کم‌رنگ می‌شوند؛ کاراکتر انتخابی همزمان با چهار نشانه خوانده می‌شود: حلقهٔ رنگی، زمینهٔ ملایم، تیک گوشهٔ کارت و نام پررنگ. همان حالت روی صفحه‌های لمسی هم دیده می‌شود (بدون تکیه بر hover)، و با کیبورد هم می‌شود با کلیدهای جهت انتخاب کرد.

### Changed

- **«چی تغییر کرد؟» دیگر به تازه‌وارد نشان داده نمی‌شود.** این دیالوگ دو نسخه را مقایسه می‌کند و برای کسی که نسخهٔ قبلی را ندیده معنایی ندارد، پس حالا فقط برای کسی می‌آید که واقعاً به‌روزرسانی کرده باشد؛ در اولین اجرا نسخهٔ فعلی ثبت می‌شود تا انتشار بعدی به همان کاربر اعلام شود. تازه‌وارد هم اگر خواست می‌تواند از پروفایل («بخونم») همین متن را خودش باز کند.
- **متن دیالوگ تغییرات ساده‌تر و خودمونی‌تر شد.** این دیالوگ حالا فقط یک کار دارد: به کسی که از نسخهٔ قبلی می‌آید بگوید چه چیزهایی عوض شده، به زبان ساده و با حال‌وهوای خود دفترچه — نهشعار و نه زبان گزارش انتشار. کارت اول آمد و آخر رفت: «دفترچه به‌روز شد» و چهار جملهٔ کوتاه.
- **کاربر تازه‌وارد هیچ راهی به دیالوگ تغییرات ندارد.** آن ردیف در پروفایل که دکمهٔ «بخونم» را داشت برداشته شد و جای آن فقط شمارهٔ نسخه مانده؛ دیالوگ تغییرات هیچ دکمهٔ ورودی در رابط ندارد و تنها راه باز شدنش همان بررسی راه‌اندازی است. پس «چی تغییر کرد؟» دقیقاً برای همان کسی می‌آید که باید بیاید.
- **پیام به‌روزرسانی روشن‌تر و بی‌کارتر شد.** متنش حالا همان یک جمله است («نسخه جدید اومده! دریافتش کن») و دکمه‌اش یک دکمهٔ پُر با ارتفاع لمس ۴۴px، نه یک لینک رنگی. کارِ اضافه هم کم شد: هر کارگر منتظر فقط یک‌بار اعلام می‌شود (مرورگر همان کارگر را دو بار گزارش می‌کند) و تا وقتی صفحه روی نمایش نیست هیچ توستی بازی نمی‌شود — به‌جایش برای لحظه‌ای که خواننده برمی‌گردد نگه داشته می‌شود، و تحویل بی‌صدای پس‌زمینه سر جای خودش است.
- **پیام به‌روزرسانی دیگر وسط کار نمی‌افتد.** پیش از این توست همان لحظه‌ای که کارگر تازه آماده می‌شد بالا می‌آمد؛ حالا منتظر اولین لحظهٔ آزاد می‌ماند: تا وقتی کاربر دستش روی کیبورد است (و تا چند ثانیهٔ سکوت بعدش)، تا وقتی یک جلسهٔ تمرکز در جریان یا در مکث است، تا وقتی رویه‌ای باز است، تا وقتی توست بازگردانی تازه یک حذف گوشهٔ صفحه را گرفته، و تا وقتی صفحه روی نمایش نیست. در این فاصله پیام دور ریخته نمی‌شود: نگه داشته می‌شود و همان اولين لحظهٔ آزاد می‌آید — پس به‌روزرسانی‌ای که نیمهٔ شب می‌رسد، صبح سر وقت خودش اعلام می‌شود.
- **یادآورهای کارها هم وقت‌شناس شدند.** یادآور مهلت دیگر وسط تایپ، وسط جلسهٔ تمرکز یا روی یک رویهٔ باز زنگ نمی‌زند و در اولین لحظهٔ آزاد می‌آید. در این فاصله یادآور دور ریخته نمی‌شود: تا وقتی زمانش نرسیده هیچ‌چیز «اعلام‌شده» ثبت نمی‌شود، پس هر بار دوباره پیدا می‌شود و اگر برنامه بسته شود، در باز شدن بعدی می‌آید. (این با اعلان به‌روزرسانی فرق دارد: آن یکی وقتی صفحه روی نمایش نیست منتظر می‌ماند، ولی یادآور از پس‌زمینه بیاید درست است — کسی که به برنامه نگاه نمی‌کند فقط با همین اعلان خبردار می‌شود.)
- **قاعدهٔ «الان وسط کاری هستم؟» یک‌جا نوشته شد.** همان سه شرط (دست روی کیبورد، جلسهٔ تمرکز، رویهٔ باز) در `js/calm.js` است و هم اعلان به‌روزرسانی و هم یادآورها از همان می‌پرسند؛ پس فردا اگر شرط چهارمی اضافه شود، هر دو با هم یاد می‌گیرند.
- **دیالوگ تغییرات نسخه کد خصوصی خودش را از دست داد.** همان سه رفتار (نگه‌داشتن Tab، قفل اسکرول و برگرداندن فوکوس) که در این فایل تکرار شده بود، حالا مال همان جای مشترک است و این دیالوگ فقط باز و بسته می‌شود.
- **صفحهٔ پروفایل دیگر برای هر تغییر خودش را از نو نمی‌سازد.** کاراکتر، عکس، چهار کارت آمار و شش نشان فقط وقتی بازنویسی می‌شوند که واقعاً عوض شده باشند؛ پایان فلاش‌زدن آواتار و نشان‌ها هنگام تغییر تم یا تغییر نام. شبکهٔ انتخاب کاراکتر هم یک‌بار ساخته می‌شود و کلید خوردن آن یک شنوندهٔ واحد دارد، پس باز کردنش دیگر SVG کاراکترها را از نو نمی‌سازد و هالهٔ انتخاب، جای خودش را نگه می‌دارد.
- **انتخاب کاراکتر حالا با تأیید انجام می‌شود.** تپ روی هر کاراکتر فقط نشانه را جابه‌جا می‌کند و دکمهٔ «انتخاب» زیر فهرست آن را ثبت می‌کند؛ پیش از این یک لمس گذرا همان لحظه کاراکتر را عوض می‌کرد و راهی نبود فهرست را ببینی و دست‌نخورده برگردی. دکمه تا وقتی چیزی برای تأیید نباشد خاموش است، و «بستن»، Escape و کلیک روی پرده انتخاب تأییدنشده را رها می‌کنند. انتخاب با کیبورد هم همان‌طور است: هر چه کلیدهای جهت رویش بایستد فقط مرحله می‌شود.
- **نشانهٔ کاراکتر انتخابی مینیمال‌تر شد.** حلقهٔ دوم دور کارت و درخشش دورش برداشته شد — یک کارت، سه راه برای گفتن یک چیز بود. کارت انتخابی حالا فقط یک حاشیهٔ لهجه، زمینهٔ ملایم، نام هم‌رنگ لهجه و یک تیک کوچک دارد. هاله به‌عنوان نشانگر hover می‌ماند (یک خط نازک دور کارتی که نشانگر رویش است) و دور کارت انتخاب‌شده دیگر تشکیل نمی‌شود، چون آن کارت نشانهٔ خودش را دارد.
- **رابط کاربری روان‌تر و سبک‌تر شد.** کارهای همیشگیِ رابط کمتر شد تا حس نرمی و آرامش بدهد، بدون تغییر در ظاهر و رفتار: شعاع بلور نوار ناوبری کم و روی لایهٔ خودش ثابت شد تا اسکرول هر فریم آن را دوباره محاسبه نکند، دو حلقهٔ بی‌پایانِ پرهزینه (هالهٔ کارهای مهم و نفسِ حلقهٔ پیشرفت) از سایه به حلقهٔ ترسیمی روی compositor منتقل شدند، و همهٔ انیمیشن‌های محیطی وقتی برنامه روی صفحه نیست متوقف می‌شوند — پس گوشی زیر بار کاری که دیده نمی‌شود فریم نمی‌کشد.
- **ورود برنامه بدون پرش انجام می‌شود.** تم انتخابی پیش از اولین رنگ‌آمیزی روی ریشه نوشته می‌شود (دیگر حالت تاریک با فلاش کاغذ روشن باز نمی‌شود)، قلم رابط هم‌زمان با استایل‌ها پیش‌بارگذاری می‌شود، و گراف ماژول‌ها از همان ابتدای پارس شدن صفحه شروع به دریافت می‌کند.
- **جابه‌جایی بین صفحه‌ها فوری است، نه با اسکرول نرم.** پیمایش نرم سراسری حذف شد؛ جهش به بالای صفحهٔ تازه بی‌درنگ انجام می‌شود تا با انیمیشن ورود همان صفحه رقابت نکند. پرش‌های درون‌لیستی (پیدا کردن کار) همان اسکرول نرم را نگه داشته‌اند، چون آنجا حرکت خودش هدف است.
- **فهرست امروز به‌جای ساختن دوباره، در جای خود به‌روز می‌شود.** تیک زدن یک کار دیگر کل فهرست را بازنمی‌سازد و انیمیشن ورود را برای همه تکرار نمی‌کند؛ فقط همان ردیف تغییر می‌کند و ردیف‌تازه‌ها وارد می‌شوند. کارت پیشنهاد هم اگر پیشنهادش عوض نشده باشد دست‌نخورده می‌ماند.
- **فهرست کارها یک‌بار و یکجا ساخته می‌شود.** ردیف‌ها در یک fragment ساخته و با یک درج وارد می‌شوند و پلهٔ انیمیشن ورودشان سقف دارد؛ فهرست بلند دیگر تا بیش از یک ثانیه به‌آرامی بالا نمی‌آید.
- **اندازه‌گیری‌های پرهزینه به هر فریم محدود شدند.** قرینهٔ ناوبری و قرص فیلتر هنگام تغییر اندازهٔ پنجره یک‌بار در هر فریم اندازه‌گیری می‌شوند، نه یک‌بار برای هر رویداد.
- **حلقهٔ پیشرفت فقط وقتی عدد عوض شود می‌چرخد.** تغییرهایی که درصد را جابه‌جا نمی‌کنند (تغییر نام، دسته یا مهلت) دیگر انیمیشن شمارش و بازرسم حلقه را از نو شروع نمی‌کنند.
- **کاربرانی که شفافیت کمتر می‌خواهند، شیشهٔ صاف می‌گیرند.** با `prefers-reduced-transparency` بلور پشت نوار ناوبری، پردهٔ دیالوگ‌ها و نوار بالای خواننده کنار می‌رود و همان سطح کاغذی مات می‌نشیند.
- **کارهای مهم هالهٔ آرام‌تری دارند.** تپش نامتقارن (باز شدن و یک دم سکوت) جای نبض رفت‌وبرگشتی را گرفت؛ همان معنا، حالِ آرام‌تر.
- **یک منبع نسخه.** نسخهٔ محصول و نام build آفلاین فقط در `js/version.js` نوشته می‌شوند؛ صفحه، Service Worker، برچسب نسخه در تنظیمات، دیالوگ تغییرات و نام کش‌ها همه از همان یک فایل می‌خوانند. شمارهٔ مستقل و دستی در Service Worker حذف شد.
- نصب کارگری که فقط نشانگر نسخه را جلو برده باشد، کش پوسته را بازنویسی نمی‌کند و فقط نشانگر را تازه می‌خواند.
- **Web Push از مسیر عادی انتشار بیرون آمد.** سیستم اعلان رسمی نسخهٔ فعلی یادآور محلی است. اشتراک خودکار Web Push که همراه زنگ یادآور کار ساخته می‌شد و بازخوانی اشتراک هنگام بالا آمدن اپ غیرفعال شدند، پس دیگر هیچ درخواستی به سرور push نمی‌رود. ماژول `js/push-service.js` و سرور اختیاری دست‌نخورده به‌عنوان زیرساخت موکول باقی مانده‌اند.
- **«چی تغییر کرد؟» فقط تازه‌ها را نشان می‌دهد.** این دیالوگ پیش از این کل سابقه را می‌آورد؛ حالا فقط ورودی‌های جدیدتر از نسخهٔ تأییدشدهٔ همان دستگاه را نشان می‌دهد، پس کسی که نسخهٔ قبل را دیده فقط همین یکی را می‌بیند و کسی که یک انتشار را جا انداخته، آن را هم می‌گیرد.
- **حلقهٔ پیشرفت و نمودار هفتگی یک مالک پیدا کردند.** حلقه که درصد همهٔ کارها را نشان می‌داد حالا نرخ تکمیل بازهٔ انتخابی را روی صفحهٔ آمار نشان می‌دهد؛ نمودار هفتگی و نشان زنجیره هم از `week.js` به `stats.js` منتقل شدند، چون هیچ‌وقت فقط درباره «این هفته» نبودند.

### Fixed

- **در نصب تازه، دیالوگ تغییرات روی کارت اسم نمی‌افتد.** هر دو لحظه‌های خوش‌آمدگویی‌اند و پیش از این هم‌زمان باز می‌شدند؛ حالا وقتی اسمی ثبت نشده باشد بررسی تغییرات منتظر می‌ماند و دیالوگ بعد از تمام شدن کارت اسم می‌آید — چه با نوشتن نام، چه با «بعداً می‌گم». کسی که از قبل اسم دارد، همان‌طور که بود، سر وقت خودش می‌بیندش.
- **فوکوس در رویه‌های باز گیر نمی‌افتد و صفحهٔ پشتشان تکان نمی‌خورد.** تا پیش از این، با کلید Tab می‌شد از یک رویهٔ باز به کنترل‌های صفحهٔ پشتش رفت و همان‌جا گم شد؛ فقط دیالوگ تغییرات این کار را برای خودش انجام می‌داد. حالا یک جای مشترک (js/modal.js) هر رویهٔ باز را می‌بیند — کارت اسم، تاس، انتخاب مهلت، هدف مطالعه، انتخاب کاراکتر، تغییرات نسخه و دو کشوی خواننده — کلید Tab را داخل همان رویه نگه می‌دارد، اسکرول صفحهٔ پشت را قفل می‌کند، و وقتی رویه بسته شد فوکوس را به همان دکمه‌ای برمی‌گرداند که بازش کرده بود (مثلاً قرص مهلت همان کار). رویه‌ای که بعداً اضافه شود هم بدون یادآوری، همین رفتار را دارد.
- **رویه‌ها برای صفحه‌خوان هم رویه اعلام می‌شوند.** هر کارت (و هر کشو) حالا `role="dialog"`، `aria-modal` و برچسب از تیتر دیدنی خودش دارد، همان قراردادی که پیش‌تر فقط برای انتخاب مهلت رعایت می‌شد.
- **ضخامت قلم دست‌آزاد پس از بازترسیم حفظ می‌شود.** ضخامت به‌صورت عددی ذخیره می‌شد ولی مسیر بازترسیم آن را با نام معنایی جست‌وجو می‌کرد و به پیش‌فرض برمی‌گشت. حالا مسیر رندر مقدار عددی را مستقیم مصرف می‌کند و رکوردهای قدیمی با نام (`thin` / `medium` / `thick`) هم سازگار خوانده می‌شوند.
- **کلیک روی اعلان در انتشار روی زیرمسیر به مسیر درست می‌رسد.** مقصد کلیک نسبت به دامنه ساخته می‌شد و روی زیرمسیر به صفحهٔ ریشهٔ دامنه می‌رفت. حالا مقصد، آیکون و badge نسبت به محل خود Service Worker ساخته می‌شوند و روی ریشه و زیرمسیر هر دو درست‌اند.
- **دکمهٔ حذف کار با کیبورد در دسترس است.** دکمهٔ حذف فقط با hover والد دیده می‌شد و نشانگر فوکوس نداشت. حالا با فوکوس داخل ردیف نمایان می‌شود و `:focus-visible` واضح دارد؛ رفتار hover دسکتاپ و ناحیهٔ لمس موبایل و منطق حذف و بازگردانی تغییری نکرده است.
- **«پاک کردن همهٔ داده‌ها» کتاب‌ها و موسیقی بارگذاری‌شده را هم پاک می‌کند.** اتصال باز به IndexedDB باعث می‌شد درخواست حذف دیتابیس بلوکه شود و فایل‌های PDF یتیم روی دستگاه بمانند، و دیتابیس موسیقی هرگز پاک نمی‌شد. حالا اتصال‌ها پیش از حذف بسته می‌شوند، شکست پاک‌سازی بی‌صدا نمی‌ماند و پس از تکمیل، دوباره بارگذاری می‌شود.
- **نوار هدف مطالعه از ثبت صفحه پر می‌شود، نه از ورق زدن.** پیشرفت کتاب، درصد و وضعیت تکمیل از شمار صفحه‌های خوانده‌شده می‌آید و همان مقدار شمار روزانهٔ هدف را هم تغذیه می‌کند، پس این دو دیگر عدد متفاوت نشان نمی‌دهند.
- **یک بارگذاری، یک build است.** صفحه و دارایی‌های هم‌منشأ آن از کش همان انتشار سرو می‌شوند، پس در بازهٔ میان انتشار و فعال شدن کارگر جدید، HTML تازه با ماژول‌های قدیمی جفت نمی‌شود.
- **باز شدن دَفتَرچه دیگر به موتور نمایش PDF گره نخورده است.** موتور رندر فقط وقتی درخواست می‌شود که کتابی باز شود؛ پیش‌تر در مسیر راه‌اندازی برنامه وارد می‌شد و اگر آن سرویس بیرونی در دسترس نبود، کل برنامه بالا نمی‌آمد. حالا صفحه‌ها بدون آن هم می‌آیند و در صورت در دسترس نبودن، خواننده پیام روشنی می‌دهد و همان کتاب را با نمایشگر ساده باز می‌کند.
- **یک مقدار خراب در حافظهٔ مرورگر دیگر جلوی بالا آمدن برنامه را نمی‌گیرد.** متادیتای کتابخانه از همان مسیر محافظت‌شدهٔ لایهٔ ذخیره‌سازی خوانده می‌شود؛ مقدار نامعتبر به قفسهٔ خالی برمی‌گردد و برنامه سالم بوت می‌شود.
- **تلاش دوبارهٔ موتور نمایش PDF در همان نشست واقعاً انجام می‌شود.** پس از یک قطعی شبکه، دکمهٔ «نمایشگر داخلی» همان آدرس قبلی را دوباره می‌خواست و مرورگر آن شکست را برای همان آدرس به خاطر می‌سپرد، پس تا رفرش صفحه هیچ درخواست تازه‌ای فرستاده نمی‌شد. حالا اولین بارگذاری همان آدرس همیشگی را می‌خواهد و فقط تلاش دوباره با یک نشانهٔ تازه انجام می‌شود؛ در نتیجه کتاب بدون رفرش به نمایشگر داخلی برمی‌گردد.
- **مقدار خارج از قرارداد حال، دیگر نمودار هفته را نمی‌شکند.** حال فقط اگر عددی در بازهٔ ۱ تا ۵ باشد خوانده می‌شود و هر مقدار دیگر مثل «حالی ثبت نشده» رفتار می‌کند؛ پیش‌تر یک مقدار خراب در حافظهٔ مرورگر هنگام ساخت نمودار خطا می‌داد.
- **ردیف کارها در همهٔ عرض‌ها خوانا شد.** چیدمان ردیف به عرض واقعی ستون محتوا (حداکثر ۴۸۰px) گره خورد، نه به عرض نمایشگر؛ پیش از این در نمایشگرهای پهن‌تر از ۴۸۰px عنوان کار به چند پیکسل فشرده می‌شد و هر حرف در یک خط می‌افتاد.
- **بازه‌های آماری با تغییر ساعت جابه‌جا نمی‌شوند.** روزهای بازه با تقویم شمرده می‌شوند، نه با ضرب ۸۶۴۰۰۰۰۰ میلی‌ثانیه؛ در مناطقی که ساعت تابستانی دارند، بازه دیگر یک روز از اسمش عقب نمی‌افتد.

## 2.0.0

### Added

- چهرهٔ تازه برای کل محصول: صفحه‌ها، کارت‌ها، دکمه‌ها و حالت تاریک بازطراحی شده‌اند.

### Changed

- دکمه‌ها روی صفحهٔ گوشی لمس‌پذیرتر شده‌اند.
- انتخاب کار برای جلسهٔ تمرکز حالا مثل یک دفترچهٔ باز و بسته می‌شود.
- حالت تاریک شب‌ها ملایم‌تر و چشم‌نوازتر شد.

> انتشارهای پیش از 2.0.0 در این فایل ثبت نشده‌اند؛ سابقه از این پس با هر انتشار کامل می‌شود.
